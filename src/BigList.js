import React, { PureComponent } from 'react'

export default class BigList extends PureComponent {
   
    
    render() {
      
        return (
            <div>
            
                <button onClick={()=>alert(1)}>abc</button>
            </div>
        )
    }
}
